# Test: closure escapes via return (heap/ARC)
# make_adder :: Int => (Int -> Int)

make_adder :: Int => (Int -> Int)
lambda n: lambda x: + x n

action main
    let add5 := make_adder 5
    let add10 := make_adder 10
    let a := add5 3
    let b := add10 7
    echo! a
    echo! b
